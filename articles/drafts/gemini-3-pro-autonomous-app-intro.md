# 【開発者必見】Gemini 3 Pro APIでつくる「自律型AIアプリ」入門！コード生成もここまで来た🛠️

## 冒頭
こんにちは！AIクリエイターのinstkoniです。

普段は**「異世界の音楽家 / AI Motion Musics」**というYouTubeチャンネルで、AI音楽やMVを制作・発信しています。

【記事概要をPodcastでも聴けます‼️】
（※実際の記事ではPodcast音声プレイヤーを埋め込みます）

> **「AIアプリを作りたいが、実装が難しそう」**
> **「コード生成がどこまで自動化できるのか知りたい」**
> **「Gemini 3 Pro の API を活用したい」**

2026年、Gemini 3 Pro の API がフルオープンになり、**自律型AIアプリ**を数行のコードで構築できる時代が到来しました。本記事では、Gemini 3 Pro API の概要、セットアップ手順、実際にコード生成を体験できるサンプルアプリを作成するフローを徹底解説します‼️

## 目次
1. Gemini 3 Pro API の全体像と特徴
2. 開発環境の構築手順（MacBook + VS Code）
3. API キー取得と認証設定
4. サンプルプロジェクト：自律型チャットアシスタントの実装
5. コード生成の活用例とベストプラクティス
6. デプロイとスケーリング
7. まとめと次のアクション

## 1. Gemini 3 Pro API の全体像と特徴
- **エンドポイント**: `https://api.gemini3pro.google.com/v1/`（REST + gRPC）
- **主要機能**:
  - **テキスト生成**（指示遵守、長文生成）
  - **コード生成**（Python, JavaScript, Swift など）
  - **マルチモーダル**（画像・音声入力 → テキスト/コード）
  - **リアルタイムストリーミング**（WebSocket）
- **認証**: API キー + OAuth2（スコープ `gemini3pro.full_access`）
- **レートリミット**: 60 RPM（リクエスト/分）
- **料金**: 生成トークン 1 M あたり $0.02、コード生成は 1 M トークン $0.03

> **ポイント**: ローカルでの **4 ビット量子化モデル** が提供され、GPU がなくても M1/M2 Mac で高速に推論可能です。

## 2. 開発環境の構築手順（MacBook + VS Code）
### 🟢 Step 1：Homebrew と Python のインストール
```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
brew install python@3.12
python3 -m venv ~/gemini_env
source ~/gemini_env/bin/activate
pip install --upgrade pip
```
### 🟢 Step 2：VS Code と拡張機能のセットアップ
- VS Code をインストール `brew install --cask visual-studio-code`
- 拡張機能 **Python**, **Pylance**, **GitHub Copilot**（参考）をインストール。
### 🟢 Step 3：Gemini SDK のインストール
```bash
pip install gemini3pro-sdk
```
SDK は REST と gRPC の両方に対応し、ストリーミングも簡単に扱えます。

## 3. API キー取得と認証設定
1. Google Cloud Console にログインし、**Gemini 3 Pro API** を有効化。
2. **認証情報** → **API キー** を作成し、`GEMINI_API_KEY` 環境変数に設定。
```bash
export GEMINI_API_KEY="YOUR_API_KEY_HERE"
```
3. （オプション）OAuth2 フローが必要な場合は `gcloud auth application-default login` を実行。

## 4. サンプルプロジェクト：自律型チャットアシスタントの実装
### 🟢 Step 1：プロジェクト構造
```
my_gemini_app/
├── main.py
├── requirements.txt
└── utils.py
```
### 🟢 Step 2：requirements.txt
```
gemini3pro-sdk
fastapi
uvicorn
python-dotenv
```
### 🟢 Step 3：utils.py（API 呼び出しラッパー）
```python
import os
from gemini3pro_sdk import GeminiClient

client = GeminiClient(api_key=os.getenv("GEMINI_API_KEY"))

def generate_text(prompt: str, max_tokens: int = 500):
    response = client.generate(
        model="gemini-3-pro",
        prompt=prompt,
        max_output_tokens=max_tokens,
        temperature=0.7,
    )
    return response.text

def generate_code(prompt: str, language: str = "python", max_tokens: int = 300):
    response = client.generate_code(
        model="gemini-3-pro-code",
        prompt=prompt,
        language=language,
        max_output_tokens=max_tokens,
    )
    return response.code
```
### 🟢 Step 4：main.py（FastAPI アプリ）
```python
from fastapi import FastAPI, Request
from utils import generate_text, generate_code
import json

app = FastAPI()

@app.post("/chat")
async def chat(request: Request):
    data = await request.json()
    user_msg = data.get("message", "")
    # 自律的に次のアクションを判断（簡易版）
    if user_msg.lower().startswith("code:"):
        # コード生成リクエスト
        code_prompt = user_msg[5:].strip()
        code = generate_code(code_prompt)
        return {"type": "code", "content": code}
    else:
        # テキスト応答
        reply = generate_text(user_msg)
        return {"type": "text", "content": reply}
```
### 🟢 Step 5：ローカルで実行
```bash
uvicorn main:app --reload
```
ブラウザで `http://127.0.0.1:8000/docs` を開くと、Swagger UI で `/chat` エンドポイントを試せます。

## 5. コード生成の活用例とベストプラクティス
| シナリオ | プロンプト例 | 期待出力 |
|----------|--------------|----------|
| **データ取得** | `code: Python で GitHub API を呼び出し、リポジトリ一覧を取得する関数を書いて` | 完全な関数定義とエラーハンドリング付きコード |
| **UI コンポーネント** | `code: SwiftUI でシンプルなカウンター画面を作成してください` | SwiftUI ビューと状態管理コード |
| **テスト自動生成** | `code: 上記の FastAPI エンドポイント用の pytest テストを書いて` | pytest 関数とモックコード |

**ベストプラクティス**:
- **具体的に指示**: 言語、フレームワーク、依存関係を明示。
- **出力制限**: `max_output_tokens` を設定し、過剰生成を防止。
- **検証**: 生成コードは必ず lint (`flake8`) とテストで検証。
- **セキュリティ**: API キーはコード内にハードコーディングせず、環境変数で管理。

## 6. デプロイとスケーリング
1. **Docker 化**
```dockerfile
FROM python:3.12-slim
WORKDIR /app
COPY . .
RUN pip install -r requirements.txt
ENV GEMINI_API_KEY=${GEMINI_API_KEY}
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```
2. **クラウドデプロイ**: Google Cloud Run、AWS Lambda（コンテナ）どちらでも簡単にスケール可能。
3. **モニタリング**: Cloud Logging と Cloud Monitoring でリクエスト数・エラーレートを可視化。

## 7. まとめと次のアクション
- **Gemini 3 Pro API** はテキスト・コード生成をシームレスに統合でき、数行のプロンプトで自律型アプリが構築可能です。
- **開発フロー** は環境構築 → API キー設定 → SDK 利用 → FastAPI でエンドポイント実装 → Docker デプロイ のシンプルなステップです。
- **ベストプラクティス** を守りつつ、生成コードは必ずテスト・レビューし、セキュリティに配慮してください。

**次のステップ**:
1. 上記サンプルをローカルで実行し、`/chat` エンドポイントでテキストとコード生成を体感。
2. 自分のプロジェクトに合わせてプロンプトをカスタマイズし、実装を自動化。
3. Docker 化して Cloud Run にデプロイし、スケールアウトを体験。

## タグ提案
#Gemini3Pro #API #コード生成 #自律型AI #FastAPI #Python #SwiftUI #Docker #クラウドデプロイ #AI開発 #生成AI #クリエイティブAI #テクノロジートレンド #インフラ #プログラミング #自動化
