# クイックスタートガイド

このガイドでは、SNSコンテンツ自動化システムを今すぐ使い始める方法を説明します。

---

## 🚀 5分でスタート

### 1. リポジトリをクローン(初回のみ)

Macのターミナルで:

```bash
cd /Volumes/WDBLACK_2TB/Git
git clone https://github.com/instkoni/sns-content-automation.git
cd sns-content-automation
```

---

## 📝 基本的なワークフロー

### Step 1: 記事ネタを追加

**方法A: スクリプトを使用(推奨)**

```bash
python3 scripts/add_idea.py
```

対話形式で入力:
- タイトル案
- カテゴリ
- 優先度
- メモ

**方法B: 手動で編集**

`ideas/ideas-list.md`を開いて、新規アイデアセクションに追加:

```markdown
| 002 | 2026-01-15 | AIで副業を効率化 | 副業 | 高 | 実践的な内容 |
```

**Gitにコミット:**

```bash
git add ideas/ideas-list.md
git commit -m "記事ネタ追加: AIで副業を効率化 (ID: 002)"
git push origin main
```

---

### Step 2: 調査を実施

**AIエージェント(Genspark/Manus)に指示:**

`docs/PROMPTS.md`の「調査・リサーチ用プロンプト」をコピーして使用。

**調査結果を保存:**

```bash
# 新規記事ファイルと調査ファイルを自動作成
python3 scripts/create_article.py 002
```

または手動で:
- `research/topics/research-002.md`に調査結果を保存

**Gitにコミット:**

```bash
git add research/topics/research-002.md
git commit -m "記事002: 調査完了"
git push origin main
```

---

### Step 3: 記事を作成

**AIエージェント(Genspark/Manus)に指示:**

`docs/PROMPTS.md`の「記事作成用プロンプト」をコピーして使用。

調査結果を貼り付けて、記事を生成してもらう。

**記事を保存:**

生成された記事を`articles/drafts/2026-01-15-002.md`に保存。

**Gitにコミット:**

```bash
git add articles/drafts/2026-01-15-002.md
git commit -m "記事002: 記事執筆完了"
git push origin main
```

---

### Step 4: インフォグラフィックを作成

**Gensparkに指示:**

`docs/DESIGN_GUIDE.md`の「Genspark用プロンプトテンプレート」を使用。

**保存:**

生成された画像を`assets/infographics/infographic-002.png`として保存。

**Gitにコミット:**

```bash
git add assets/infographics/infographic-002.png
git commit -m "記事002: インフォグラフィック作成"
git push origin main
```

---

### Step 5: Noteに投稿

1. 記事本文をNoteにコピー
2. インフォグラフィックをアップロード
3. CANVAでトップ画像を作成
4. タグを設定
5. 公開

**公開後:**

```bash
python3 scripts/publish_article.py 002 https://note.com/username/n/xxxxx
```

これで自動的に:
- 記事が`published/`に移動
- `ideas-list.md`のステータスが「完了」に更新

**Gitにコミット:**

```bash
git add .
git commit -m "記事002: Note公開完了"
git push origin main
```

---

### Step 6: LinkedIn/Xに投稿

**LinkedIn向けリライト:**

`docs/PROMPTS.md`の「LinkedIn向けリライトプロンプト」を使用。

生成されたテキストを`articles/published/linkedin-002.md`に保存。

**Xスレッド作成:**

`docs/PROMPTS.md`の「X(Twitter)スレッド作成プロンプト」を使用。

生成されたテキストを`articles/published/x-thread-002.md`に保存。

**Gitにコミット:**

```bash
git add articles/published/linkedin-002.md articles/published/x-thread-002.md
git commit -m "記事002: LinkedIn/X投稿準備完了"
git push origin main
```

---

## 🎯 よく使うコマンド

### 記事ネタ追加
```bash
python3 scripts/add_idea.py
```

### 新規記事作成
```bash
python3 scripts/create_article.py <記事ID>
```

### 記事公開
```bash
python3 scripts/publish_article.py <記事ID> <Note URL>
```

### Git操作
```bash
# 変更を確認
git status

# すべて追加
git add .

# コミット
git commit -m "メッセージ"

# プッシュ
git push origin main

# 最新を取得
git pull origin main
```

---

## 📋 毎日のルーティン

### 朝(10分)
1. `git pull`で最新状態に更新
2. `ideas/ideas-list.md`で今日のタスク確認
3. 優先度の高い記事ネタを選定

### 作業中(2-3時間)
1. 調査実施 → Git commit
2. 記事執筆 → Git commit
3. インフォグラフィック作成 → Git commit
4. Note投稿準備

### 夕方(30分)
1. Note公開
2. LinkedIn/X投稿
3. `git push`で1日の成果を保存
4. `workflows/workflow-checklist.md`で進捗確認

---

## 🆘 トラブルシューティング

### Q: スクリプトが動かない
```bash
# Pythonのバージョン確認
python3 --version

# 実行権限を付与
chmod +x scripts/*.py
```

### Q: Gitでエラーが出る
```bash
# 状態を確認
git status

# コンフリクトがある場合
git pull origin main
# 手動でコンフリクトを解決
git add .
git commit -m "コンフリクト解決"
git push origin main
```

### Q: ファイルが見つからない
```bash
# 正しいディレクトリにいるか確認
pwd

# sns-content-automationディレクトリに移動
cd /Volumes/WDBLACK_2TB/Git/sns-content-automation
```

---

## 📚 次に読むべきドキュメント

1. **[USAGE.md](USAGE.md)**: 詳細な使い方
2. **[PROMPTS.md](PROMPTS.md)**: AIエージェント用プロンプト集
3. **[DESIGN_GUIDE.md](DESIGN_GUIDE.md)**: デザインガイドライン
4. **[workflow-checklist.md](../workflows/workflow-checklist.md)**: 詳細なワークフロー

---

## 💡 効率化のヒント

1. **プロンプトをブックマーク**: よく使うプロンプトはブラウザにブックマーク
2. **テンプレートを活用**: 毎回同じ構造で作成することで時間短縮
3. **バッチ処理**: 複数の記事を同時進行で効率化
4. **定期的なレビュー**: 週次でワークフローを見直し

---

## 🎉 これで準備完了!

さっそく最初の記事ネタを追加して、ワークフローを試してみましょう!

```bash
python3 scripts/add_idea.py
```
